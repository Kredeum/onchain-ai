{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "lib/**",
        "../chainlink/config.json",
        "../foundry/addresses.json"
      ],
      "outputLogs": "new-only"
    }
  }
}
