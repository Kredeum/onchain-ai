{
  "extends": ["//"],
  "tasks": {
    "format": {
      "inputs": ["src/**", "../chainlink/config.json", "../foundry/addresses.json"],
      "outputs": ["../svelte5/src/lib/deployments.json"],
      "outputLogs": "new-only"
    }
  }
}
